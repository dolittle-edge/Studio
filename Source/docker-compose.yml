version: '3'
services:
  dolittle-edge-core:
    image: dolittle.azurecr.io/edge/core:latest
    build:
      context: "."
      dockerfile: "Core/Dockerfile"

  dolittle-edge-web:
    image: dolittle.azurecr.io/edge/web:latest
    build:
      context: "."
      dockerfile: "Web/Dockerfile"
